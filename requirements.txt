fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
langgraph>=0.2.28
langchain-core>=0.3
langchain-openai>=0.2
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0

# 추월 예측 모델 추론(app/ml) — 저장된 LightGBM 부스터 로드·예측·보정
lightgbm>=4.0
numpy>=1.24

# dev
pytest>=8.0
pytest-asyncio>=0.23

# 음성(선택) — 필요할 때만, 되도록 Python 3.11 별도 venv에서 설치.
#   (MeloTTS/torch는 Python 3.14 미지원. PyPI 'melotts'는 깨진 패키지 → GitHub로 설치)
# TTS: pip install git+https://github.com/myshell-ai/MeloTTS.git  (+ python -m unidic download)
# STT: pip install faster-whisper
